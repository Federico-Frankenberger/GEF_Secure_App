# Dependencias
node_modules/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Entorno
.env
.env.local
.env.*.local

# Build
dist/
build/

# Gradle (CFG-01, docs/20-08-26/AUDITORIA_END_TO_END.md): backend/.gradle/ quedó
# versionado en algún commit anterior a que esto existiera acá -- son binarios de caché
# de build local que se regeneran solos, generan ruido de diff en cada build de cada
# desarrollador. No se hace "git rm --cached" en esta corrida (reescribe qué hay
# trackeado, se confirma aparte); esto solo evita que se agreguen más cambios de ahí en
# adelante.
.gradle/

# Archivos del sistema
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/

# DT-06 (docs/deuda-tecnica.md): destino por defecto de scripts/backup.sh --
# dumps de Postgres y el volumen de n8n, nunca deberían versionarse en git.
backups/